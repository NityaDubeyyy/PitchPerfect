{
  "buildCommand": "cd pitchperfect/client && npm install && npm run build",
  "outputDirectory": "pitchperfect/client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
